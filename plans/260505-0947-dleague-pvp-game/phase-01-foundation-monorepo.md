---
phase: 1
title: "Foundation & monorepo"
status: pending
priority: P1
effort: 1w
dependencies: []
---

# Phase 1: Foundation & monorepo

## Overview

Set up the Go workspace monorepo, Ebitengine WASM scaffold, **protobuf schema layer (`buf` tooling)**, Postgres dev environment, and CI pipeline. Outcome: `git clone && make dev` brings up a hello-world Ebitengine canvas + a Go server with `/health` and a `/ws` WebSocket endpoint that exchanges protobuf-encoded ping/pong.

> **Porting reference:** see `reports/xia-260505-1014-core-extraction.md` for source attributions and decisions.

## Requirements

- **Functional:**
  - `go.work` workspace with `client/`, `server/`, `shared/` modules
  - `client/` builds to `dist/wasm/main.wasm` via `GOOS=js GOARCH=wasm`
  - `server/` runs on `:8080` with `/health` (HTTP) and `/ws` (WebSocket upgrade)
  - `shared/pb/` contains generated protobuf Go code (committed)
  - `proto/dleague/v1/` holds `.proto` schema sources + `buf.yaml` + `buf.gen.yaml`
  - `make proto-gen` regenerates `shared/pb/` from `proto/`
  - Postgres runs locally via `docker-compose.yml`
  - `make dev` starts server + builds WASM
  - GitHub Actions CI: `proto-lint`, `proto-breaking`, `golangci-lint`, `go test ./...`, build WASM
- **Non-functional:**
  - WASM bundle <8MB at this phase (hello world + protobuf-go runtime)
  - Each Go file <200 LOC
  - kebab-case directories, snake_case Go files
  - Apache-2.0 attribution preserved on directly-ported files (Ebitengine examples)

## Architecture

```
dleague/
├── client/                  # Ebitengine WASM
│   ├── cmd/web/main.go      # entry point
│   ├── internal/
│   │   ├── scene/           # title, game, results scenes
│   │   ├── ui/              # HTML overlay bridge (syscall/js)
│   │   ├── game/            # client-side game state
│   │   └── net/             # WS client wrapper
│   └── go.mod
├── server/                  # Go HTTP + WebSocket
│   ├── cmd/api/main.go
│   ├── internal/
│   │   ├── http/            # static + /ws upgrade only
│   │   ├── ws/              # WebSocket hub + dispatch
│   │   ├── store/           # Postgres repo (Phase 3+)
│   │   └── config/
│   └── go.mod
├── shared/                  # cross-module types + interfaces
│   ├── pb/                  # GENERATED protobuf code (committed)
│   │   └── dleague/v1/
│   ├── game/                # Game interface (pluggable -dle) + Registry
│   └── go.mod
├── proto/                   # protobuf source schemas
│   ├── dleague/v1/
│   │   └── envelope.proto   # Envelope{type, request_id, payload}
│   ├── buf.yaml
│   ├── buf.gen.yaml
│   └── buf.lock
├── web/                     # static HTML shell + CSS overlay
│   ├── index.html
│   ├── wasm_exec.js         # from Go SDK
│   └── styles.css
├── docker-compose.yml       # postgres only at this phase
├── go.work
├── NOTICE                   # third-party attributions (already exists)
├── LICENSE                  # proprietary (already exists)
├── README.md                # already exists
├── Makefile
└── .github/workflows/ci.yml
```

## Related Code Files

**Create:**
- `go.work`
- `proto/buf.yaml`, `proto/buf.gen.yaml`, `proto/dleague/v1/envelope.proto`
- `client/go.mod`, `client/cmd/web/main.go`, `client/internal/scene/title.go`, `client/internal/net/ws_client.go`
- `server/go.mod`, `server/cmd/api/main.go`, `server/internal/http/router.go`, `server/internal/http/health.go`, `server/internal/ws/hub.go`, `server/internal/ws/conn.go`
- `shared/go.mod`, `shared/game/game.go` (interface), `shared/game/registry.go` (factory pattern, adapted from ratel-online state.register)
- `shared/pb/dleague/v1/envelope.pb.go` (generated by buf)
- `web/index.html`, `web/wasm_exec.js`, `web/styles.css`
- `docker-compose.yml` (postgres:16)
- `Makefile`
- `.github/workflows/ci.yml`
- `.golangci.yml`

**Modify:** README.md (append `make proto-gen` step in quickstart)

## Implementation Steps

1. `go work init` → `go work use ./client ./server ./shared`
2. Scaffold `proto/buf.yaml` (lint preset DEFAULT, breaking preset FILE), `buf.gen.yaml` (plugin: `go` with paths source_relative)
3. Write `proto/dleague/v1/envelope.proto` — `Envelope{MessageType type, string request_id, bytes payload}` + Ping/Pong stub messages
4. Run `buf generate` → produces `shared/pb/dleague/v1/*.pb.go`. Commit generated files
5. Scaffold `shared/game/game.go` with `Game` interface: `Init(seed) / HandleKey(k) / Tick(dt) / Render(screen) / State() / IsTerminal() / Result()`
6. Scaffold `shared/game/registry.go` with `Register(id string, factory Factory)` pattern. Attribution comment: "Pattern adapted from ratel-online/server/state/state.go (MIT)"
7. Scaffold `client/cmd/web/main.go` — Ebitengine `Game{Layout/Update/Draw}` boilerplate. Apache-2.0 header preserved at top: "Pattern adapted from hajimehoshi/ebiten/examples/2048 (Apache-2.0)"
8. Add `web/index.html` loading `wasm_exec.js` + `main.wasm`, CSS overlay placeholder div
9. Scaffold `server/cmd/api/main.go` — `chi` router. Routes: `GET /` static, `GET /static/*` embedded, `GET /health`, `GET /ws` upgrade
10. Use `nhooyr.io/websocket` (lighter than gorilla, modern API) for WS upgrade
11. Scaffold `server/internal/ws/hub.go` — minimal hub: register/unregister conn, on Ping respond with Pong (using generated proto types via protojson marshal)
12. Add `docker-compose.yml` with `postgres:16` on `:5432`, persistent volume
13. Write `Makefile` targets: `dev`, `build-wasm`, `build-server`, `test`, `lint`, `db-up`, `proto-gen`, `proto-lint`, `proto-breaking`
14. Install `buf` in dev dependencies (CI runs: `go install github.com/bufbuild/buf/cmd/buf@latest`)
15. Configure `golangci-lint` (default + `gofmt`, `revive`, `unused`)
16. CI workflow: Go 1.23, run `proto-lint` + `proto-breaking` + lint + test + build WASM, upload artifact
17. Append to README quickstart: `make proto-gen` step

## Todo List

- [ ] Init Go workspace + 3 modules
- [ ] `proto/` scaffold + `buf.yaml` + `buf.gen.yaml`
- [ ] First proto: `envelope.proto` with ping/pong
- [ ] `make proto-gen` produces `shared/pb/`
- [ ] Define `shared/game.Game` interface + `Registry` factory
- [ ] Ebitengine hello-world WASM (port `Game{Layout/Update/Draw}` w/ Apache attribution)
- [ ] HTML shell + CSS overlay scaffold
- [ ] Go server: chi router with /health + /ws upgrade
- [ ] WS hub stub: ping/pong via protobuf
- [ ] WS client wrapper in client/internal/net (syscall/js bridge)
- [ ] docker-compose Postgres
- [ ] Makefile (dev/build/test/proto-gen/proto-lint/proto-breaking)
- [ ] golangci-lint config
- [ ] GitHub Actions CI (incl proto-lint + proto-breaking)
- [ ] README quickstart updated

## Success Criteria

- [ ] `make dev` starts server on :8080, browser loads "Dleague" title screen
- [ ] `curl localhost:8080/health` returns 200 OK
- [ ] Browser WS connects to `/ws`, sends Ping, receives Pong (visible in devtools network tab)
- [ ] `make proto-gen` regenerates without diff (CI verifies)
- [ ] CI green on push: proto-lint + proto-breaking + lint + test + build WASM
- [ ] WASM bundle <8MB
- [ ] All Go files <200 LOC
- [ ] NOTICE file lists ratel-online (MIT) + Ebitengine (Apache-2.0)
- [ ] Direct-ported files carry original copyright headers

## Risk Assessment

| Risk | Mitigation |
|------|-----------|
| Ebitengine WASM tooling unfamiliar | Use official `ebitengine.org` examples as starting point |
| `wasm_exec.js` version drift with Go upgrades | Pin Go version in CI + Makefile |
| `buf` adds first-run setup friction | One-shot `go install` in Makefile + CI; document in README |
| Generated `*.pb.go` drift in PRs | CI step: `buf generate` then `git diff --exit-code` to fail on uncommitted regen |
| Workspace + 3 modules feels heavy day-1 | Keep `shared/` minimal — only types/interfaces + Registry, no logic |
| protobuf-go runtime adds ~700KB to WASM | Acceptable within <10MB total budget; track in CI |
| Air / file-watcher complexity | Skip auto-rebuild MVP; just `make build-wasm` manually |
