# Phase 04 Test Verification Report
**Date:** 2026-05-09 15:02 | **Phase:** Pluggability Decision (Proto Rename + Doc Edits + Dead Code Drop)

## Server (Go)

**Build Status:** PASS
- `go build ./server/...` — No errors
- `go vet ./server/...` — No issues

**Test Execution:** 6/10 packages with tests, all PASS
```
✓ auth          1.153s
✓ game/wordle   1.100s
✓ http          1.125s
✓ scheduler     1.230s
✓ store         1.035s
✓ ws            1.391s
```

**No-test packages (4):** cmd/admin, cmd/api, cmd/seed-wordlists, config

**Linting Issues (4 pre-existing, Phase 04 clean):**
- errcheck: 2 (router_test.go Close/CloseNow)
- gofmt: 1 (wordle.go formatting)
- revive: 1 (colors.go unexported ColorGray comment)

**Proto Validation:** BREAKING CHANGE (expected & intentional)
- Enum rename `MESSAGE_TYPE_GAME_MOVE → WORDLE_MOVE` (value 6)
- Enum rename `MESSAGE_TYPE_GAME_STATE → WORDLE_STATE` (value 7)
- Numeric values unchanged; no runtime impact on local clients
- Phase plan explicitly approved; preempts future multi-game complication

---

## Web (SvelteKit+Vitest)

**Type Check:** PASS
- `npm run check` — 0 errors, 0 warnings, 400 files validated

**Tests:** 9/9 PASS (1 test file, 243ms)
```
✓ All tests pass, 100% success rate
```

No regressions in web layer post-rename.

---

## Regressions vs Phase 05 Baseline

**Test count:** 6 packages maintained (identical to P05)
**Test results:** All passing (0 failures, 0 flakes)
**Coverage parity:** No coverage drop; store package still 0% (pre-existing, documented in P05 report)

**Proto breaking:**
- Expected: MESSAGE_TYPE enum rename (doc-planned)
- Impact: Local client/server only; no external API consumers
- Mitigation: Numeric value invariant preserves wire compatibility

**Code removal (dead-code cleanup):**
- Deleted: `server/internal/store/games.go` (fully unused, 0 call sites)
- Modified: Removed `_ = store.NewGameRepo(db)` discard
- Modified: Dropped unused `KeyEnter`/`KeyBackspace` constants from `shared/game/game.go`
- Result: No orphaned imports; builds clean

**Pluggable references cleaned:**
- Grep `MESSAGE_TYPE_GAME_MOVE`: 0 hits (all renamed)
- Grep `MESSAGE_TYPE_GAME_STATE`: 0 hits (all renamed)
- Grep `TODO(phase-XX)` closed phases: 0 hits (Phase 07 TODOs cleaned)

---

## Recommendations

1. Fix pre-existing lint issues before merge:
   - errcheck: Add error handling or `_` to router_test.go
   - gofmt: Fix wordle.go formatting
   - revive: Add comment to ColorGray export or unexport

2. Confirm `proto breaking` approval with PM (intentional enum rename, no runtime impact)

3. Phase 05 baseline gaps unchanged (store 0%, auth 14%, ws 23%, http 40%) — not P04's responsibility

---

**Status:** DONE_WITH_CONCERNS

Reason: All functional tests pass with no regressions. Dead-code cleanup correct. Proto rename intentional per plan. Four pre-existing lint issues should be fixed pre-merge, unrelated to Phase 04 scope.
